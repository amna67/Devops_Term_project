name: ci-prod-wf
on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
jobs:
  linting-job:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo by fetching SHA of branch that triggered the workflow
        uses: actions/checkout@v6
      - name: Install Development Dependencies
        run: npm install
      - name: Lint HTML Files
        run: npx htmlhint "**/*.html"
      - name: Lint CSS Files
        run: npx stylelint "**/*.css"
  app-build-job:
    runs-on: ubuntu-latest
    needs: "linting-job"
    steps:
      - name: Clone Repo by fetching SHA of branch that triggered the workflow
        uses: actions/checkout@v6
      - name: Install Development Dependencies
        run: npm install
      - name: Build Website With Parcel
        run: npx parcel build "./src/index.html" --dist-dir "./dist" --public-url "./" --no-cache   
  image-build-job:
    needs: "app-build-job"
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo by fetching SHA of branch that triggered the workflow
        uses: actions/checkout@v6
      - name: Login To Dockerhub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build Image
        run: docker image build -t ${{ secrets.DOCKERHUB_USERNAME }}/devops_term_project:prod . 
      - name: List the Images
        run: docker image ls
      - name: Push Docker Image on DH
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/devops_term_project:prod